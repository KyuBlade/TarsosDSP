apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(":desktop")
    testCompile 'junit:junit:4.12'
    testCompile project(":desktop").sourceSets.test.output
}